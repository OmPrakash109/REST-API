#In .env all variable's name should be uppercase without spaces
MONGO_URI=mongodb://0.0.0.0:27017/todoapp       #we have to specify which db it should connect to, either we create a new database and specify that or mongo can create database on the fly as well
PORT=8000                                       # .env are golbal environment variables defined at project level